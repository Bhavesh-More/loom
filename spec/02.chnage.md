Task 3 — Collapsible Chats and Projects sections
Where: Sidebar sections "Chats" and "Projects."
Required:

Add a collapse/expand toggle (chevron) to each section header, independently controlled.
Collapsing a section hides its list but keeps the header visible.
State should persist across sessions (e.g. localStorage or user preference store) so the sidebar doesn't reset on reload — confirm with existing state pattern in the app if one exists.

Acceptance criteria: Chats and Projects can each be collapsed/expanded independently without affecting the other section.

---

Task 4 — Projects: cap sidebar list at 7, overflow opens right panel
Required:

Sidebar "Projects" section shows a maximum of 7 projects.
If more than 7 exist, show a "View all" / "+N more" affordance below the 7th item.
Clicking it opens the full project list in the right panel (same right-panel-switch pattern as Task 2, not a separate modal), showing all projects.

Acceptance criteria: Sidebar never renders more than 7 project rows regardless of total count. Overflow trigger only appears when count > 7. Right panel correctly displays the full list when triggered.
