there will be select after Model Selector which has condition to apper only when streamlit agent is selected, upon clicking will open modal like other selector where user can select theme from list only one if it select then that theme is uses to construct prompt and use that colors, sizes butoon style so you get it it will use all that present in that md.


how should it suppose to work

when i give description of any porject that i want to create and select any theme from the available theme, then rathe rthan just appending the theme to prompt it should constrcut perfect prompt with using theme so that.  final ui must be stunning and beautiful to look dont just restict to this available themes i may add new themes later so make it flexible and make sure this feature is only on when streamlit agent is selcted as its responsible for ui generation.