Task 1 — Fix setActiveSession TS errors in WorkspacePage.tsx
File: loom/ide/src/pages/WorkspacePage.tsx
Instruction to agent:
Open the file, locate both usages of setActiveSession that are underlined, and inspect the exact TS diagnostic (hover tooltip or tsc output). Fix based on cause — do not silence with @ts-ignore or any. Common causes to check first:

The state setter's inferred type doesn't match the value being passed (e.g. passing string | null into a setter typed for Session, or vice versa).
setActiveSession is being called with an updater function (prev) => ... but the state type wasn't declared to accept a function overload correctly.
A stale/duplicate declaration — activeSession state declared with one type via useState<T>() but a different shape used elsewhere.
Import path mismatch if setActiveSession comes from a context/store rather than local useState.

Acceptance criteria: No red squiggles on either call site, tsc --noEmit passes clean for this file, no behavior change to session switching.

---

Task 2 — Agents: sidebar entry → right panel view
Current: Agents are listed directly in the sidebar.
Required: Sidebar shows a single clickable "Agents" row/div (icon + label), not an expanded list.
On click: Right panel switches to an "Agents" view (same panel used elsewhere for switching content, not a modal).
Right panel — empty state:

Text: "No agents attached to your account."
A link/button to the Marketplace section.

Right panel — populated state:

Horizontal list of agent cards. Each card shows:

Agent icon
Agent name
Short description, truncated with ... (CSS text-overflow: ellipsis; white-space: nowrap; overflow: hidden; or line-clamp) if it doesn't fit
A delete icon


Clicking the delete icon opens a confirmation modal with a warning message and Delete / Cancel actions. Only remove the agent on explicit confirm.
Clicking the agent's icon navigates to that agent's description page inside the Marketplace section (not the delete icon, not the whole card).

Acceptance criteria: Sidebar no longer renders the full agent list. Right panel correctly toggles between whatever it currently shows and the Agents view. Delete requires confirmation. Icon click routes to Marketplace → agent detail.